/**
 * The grid package contains lightweight domain types and behaviors for a
 * simple 2D simulation. It provides position types (`Vec2`), a basic
 * `Cell` container, and modeling abstractions for mold growth and
 * propagation. Implementations such as `FastMold` and `SlowMold`
 * demonstrate different growth rates.
 *
 * <p>These classes and interfaces are intentionally small and focused;
 * they model the minimal behavior needed by the simulation grid logic.
 */
package com.example.simul2d.grid;
